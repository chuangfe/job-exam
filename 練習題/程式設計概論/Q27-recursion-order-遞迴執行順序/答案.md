# Q27 遞迴執行順序 —— 答案

## 輸出

```
前 3
前 2
前 1
後 1
後 2
後 3
```

## 解析

- 程式先一路遞迴下去，依序印出「前 3、前 2、前 1」，直到 `n === 0` 返回。
- 接著呼叫堆疊（call stack）逐層回返，從最深的那一層開始印出「後 1、後 2、後 3」。
- 「前」的輸出（遞迴呼叫之前）是**正序**，「後」的輸出（遞迴呼叫之後）是**逆序**——這正是遞迴「去程」與「回程」的差別。

**最後更新**：2026-06-15
