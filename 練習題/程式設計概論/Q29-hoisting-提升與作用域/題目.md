# Q29 提升（Hoisting）與作用域

請預測以下程式的輸出結果：

```javascript
console.log(x);          // ?
var x = 5;

foo();                   // ?
function foo() { console.log("foo 被呼叫"); }

try {
  console.log(y);        // ?
  let y = 1;
} catch (e) {
  console.log(e.name);
}
```

問：輸出是什麼？

**最後更新**：2026-06-15
