# Q28 - 圖片懶載入（Lazy Loading）

## 問題

如何實作圖片懶載入（lazy loading）？原生做法與用 `IntersectionObserver` 的進階做法各是什麼？實作時要注意哪些細節才能避免版面跳動（CLS）？

## 引導思考

- 為什麼要懶載入圖片？對首屏載入速度和頻寬有什麼好處？
- 原生 HTML 有沒有最簡單的一行解法？它的限制是什麼？
- 要更精細控制（例如提前一段距離預載），可以用哪個 Web API 來偵測圖片是否即將進入視窗？
- 「先把真正的網址放在 `data-src`，進入視窗才設給 `src`」這個模式是怎麼運作的？
- 為什麼懶載入的圖片要事先指定寬高或 `aspect-ratio`？不指定會發生什麼（提示：CLS）？
- 圖片載入後，IntersectionObserver 還需要繼續觀察它嗎？

**最後更新**：2026-06-15
