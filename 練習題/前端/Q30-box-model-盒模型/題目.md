# CSS 盒模型（Box Model）

## 問題
1. 請說明 CSS 盒模型（Box Model）由哪些部分組成？
2. `box-sizing` 的 `content-box` 與 `border-box` 有什麼差異？
3. 為什麼許多專案會在全域加上 `* { box-sizing: border-box; }`？這樣做有什麼好處？

## 引導思考
- 當你設定一個元素 `width: 200px; padding: 20px; border: 5px solid;`，在不同 `box-sizing` 下，這個元素實際佔據的寬度各是多少？
- 哪一種設定方式在排版（例如要塞滿某個百分比寬度）時比較直覺、比較不容易算錯？

**最後更新**：2026-06-15
