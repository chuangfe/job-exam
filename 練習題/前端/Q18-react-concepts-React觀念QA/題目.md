# React 觀念 Q&A

請依序回答以下 React 核心觀念問題。建議先自己用白話講出來，再對照解析。

## Q1. 虛擬 DOM 與 reconciliation（協調）是什麼？
- 虛擬 DOM 解決什麼問題？狀態變更時 React 做了哪些事？
- React 的 diff 演算法用了哪些啟發式假設？複雜度為何？
- Fiber 架構對 reconciliation 帶來什麼能力？

## Q2. key 的作用？為何不要用 index 當 key？
- key 在列表 diff 時扮演什麼角色？
- 用 index 當 key 在什麼操作下會出問題？會造成哪些可觀察的 bug？

## Q3. useState / useEffect 如何運作？
- Hooks 為何不能寫在條件式或迴圈裡？背後的儲存機制是什麼？
- 什麼是「函式式更新」？為什麼能避免過期閉包？
- useEffect 與 useLayoutEffect 的執行時機差在哪？各自適合什麼場景？

## Q4. useEffect 的依賴陷阱與清理機制？
- 依賴陣列要放哪些值？漏放會發生什麼？
- 空陣列 `[]` 與「省略陣列」分別代表什麼？
- 清理函式在什麼時機被呼叫？典型用途有哪些？

## Q5. useMemo / useCallback / React.memo 何時用？
- 三者各自記憶什麼？
- 它們不是免費的，什麼情況下包了反而沒意義？

## Q6. Context 的重渲染問題與優化？
- Context value 變更時，誰會重渲染？
- 有哪些常見的優化手段？

## Q7. 受控 vs 非受控元件？
- 兩者差別與各自的適用情境。

## Q8. 批次更新（batching）？
- React 18 在 batching 行為上有什麼改變？
- 需要強制同步更新時要用什麼？有何代價？

**最後更新**：2026-06-15
