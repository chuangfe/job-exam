# CSS 各種置中方式

## 問題
1. CSS 中有哪些常見的「置中」做法？
2. 水平置中、垂直置中、水平+垂直同時置中，分別可以怎麼做？
3. 在「不知道子元素確切尺寸」的情況下，要如何垂直置中？

## 引導思考
- 現代佈局（flex / grid）相較於傳統做法（絕對定位、line-height）有什麼優勢？
- `transform: translate(-50%, -50%)` 為什麼可以在不知道尺寸的情況下精準置中？
- 單行文字與整個區塊的置中，使用的技巧一樣嗎？

**最後更新**：2026-06-15
