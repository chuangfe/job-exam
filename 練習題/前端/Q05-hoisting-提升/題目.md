# Hoisting（提升）

## 問題

請說明以下重點，並用程式碼佐證：

1. 什麼是 Hoisting（提升）？
2. 函式宣告（function declaration）的提升行為是什麼？能不能在宣告前呼叫？
3. `var` 的提升行為和函式宣告有什麼不同？提升後的值是什麼？
4. `let` / `const` / `class` 也會被提升嗎？什麼是 TDZ（暫時死區）？
5. 函式運算式（如 `const fn = () => {}`）的提升行為又是如何？

請思考下列程式碼的結果（哪些可以正常執行、哪些會報錯）：

```js
foo();
function foo() { console.log('foo'); }

console.log(x);
var x = 1;

console.log(y);
let y = 2;
```

**最後更新**：2026-06-15
